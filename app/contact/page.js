import Contact from "@/components/Contact/Contact";
import Layout from "@/components/Layout/Layout";

const ContactPage = () => {
  return (
    <Layout>
      <Contact sectiobTitle={"Contacts"} styles={"container"} />;
    </Layout>
  );
};

export default ContactPage;
